# Lyzr Agent - Environment Configuration
# Copy this to .env and update with your values after deployment

# ============================================================================
# AWS Configuration
# ============================================================================
AWS_PROFILE=default
AWS_REGION=us-east-1

# Resource Naming Prefix (defaults to AWS Account ID if not set)
# This prefix is used for all AWS resource names (S3 buckets, DynamoDB tables)
# to avoid naming conflicts when multiple users deploy to different accounts
# RESOURCE_PREFIX=my-company

# ============================================================================
# Deployment Values (Fill after running 'npm run deploy')
# ============================================================================

# API Gateway Endpoint (from CDK output: ApiEndpoint)
REACT_APP_API_ENDPOINT=

# Cognito Configuration (from CDK outputs)
REACT_APP_USER_POOL_ID=
REACT_APP_APP_CLIENT_ID=

# Region
REACT_APP_REGION=us-east-1

# S3 Buckets (auto-generated with prefix)
# These will be: {RESOURCE_PREFIX}-lyzr-app and {RESOURCE_PREFIX}-lyzr-vectors
# Fill these values after deployment from CDK outputs
S3_FRONTEND_BUCKET=
S3_VECTOR_BUCKET=

# ============================================================================
# Google OAuth (optional)
# ============================================================================
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# ============================================================================
# Bedrock Model Configuration (optional - override defaults)
# ============================================================================
# BEDROCK_LLM_MODEL=amazon.nova-lite-v1:0
# BEDROCK_EMBED_MODEL=amazon.titan-embed-text-v2:0
# BEDROCK_REGION=us-east-1

# Available LLM Models (text generation):
# 
# Amazon Nova (AWS Native - Cheapest):
# - amazon.nova-micro-v1:0     ($0.035/$0.14 per 1M tokens - fastest, cheapest)
# - amazon.nova-lite-v1:0      ($0.06/$0.24 per 1M tokens - DEFAULT, best value)
# - amazon.nova-pro-v1:0       ($0.80/$3.20 per 1M tokens - highest quality)
#
# Anthropic Claude (High Quality):
# - anthropic.claude-3-haiku-20240307-v1:0    ($0.25/$1.25 per 1M tokens)
# - anthropic.claude-3-5-haiku-20241022-v1:0  ($1.00/$5.00 per 1M tokens)
# - anthropic.claude-3-sonnet-20240229-v1:0   ($3.00/$15.00 per 1M tokens)
# - anthropic.claude-3-5-sonnet-20241022-v2:0 (Requires inference profile)
#
# Meta Llama (Open Source):
# - meta.llama3-1-8b-instruct-v1:0   (Small, fast)
# - meta.llama3-1-70b-instruct-v1:0  (Large, capable)
# - meta.llama3-1-405b-instruct-v1:0 (Massive, most capable)
#
# Amazon Titan:
# - amazon.titan-text-express-v1     (Legacy - fast)
# - amazon.titan-text-lite-v1        (Legacy - cheap)
# - amazon.titan-text-premier-v1:0   (Latest Titan)
#
# Available Embedding Models:
# - amazon.titan-embed-text-v1       (1536 dimensions)
# - amazon.titan-embed-text-v2:0     (1024 dimensions - DEFAULT, latest)
# - cohere.embed-english-v3          (1024 dimensions)
# - cohere.embed-multilingual-v3     (1024 dimensions)

